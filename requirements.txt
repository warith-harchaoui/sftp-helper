# sftp-helper — pip requirements (portable, no `file://` deps).
#
# For a plain install:
#     pip install -r requirements.txt
#     pip install -e .           # sftp-helper itself (editable) or `pip install .`
#
# Optional surfaces (CLI/API/MCP) live behind extras, e.g. `.[cli]`, `.[api,mcp]`.
# The os-helper pin is kept in lock-step with the release tag below.

# --- Suite dependency -----------------------------------------------------
os-helper>=1.5.3
# --- Core runtime ---------------------------------------------------------
# No Python SSH library: sftp-helper drives the system OpenSSH `sftp` client,
# so the only runtime requirement beyond os-helper is that binary on PATH.
